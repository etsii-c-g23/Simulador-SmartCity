\documentclass[conference,a4paper,onecolumn,11pt]{IEEEtran} % Formato oficial de IEEE para conferencias

\usepackage[utf8]{inputenc} % Establece la codificación UTF-8 para el documento
\usepackage[spanish, es-tabla]{babel} % Traducir etiquetas automáticas
\usepackage{geometry} % Gestión personalizada de márgenes y tamaño de página (IEEEtran ya define sus propios márgenes)
\usepackage{float} % Permite forzar la posición exacta de figuras/tablas usando el parámetro [H]
\usepackage{listings} % Permite insertar y formatear bloques de código 
\usepackage{xcolor} % Provee soporte avanzado para colores (necesario para colorear sintaxis en 'listings')
\usepackage{amsmath} % Escritura mejorada de fórmulas matemáticas
\usepackage{graphicx} % Gráficos e imágenes
\usepackage{caption} % Mejoras en las leyendas de figuras y tablas
\usepackage[kw]{pseudo} % Escritura de pseudocódigo
\usepackage{booktabs} % Escritura mejorada de tablas
\usepackage{cite} % Escritura mejorada de citas bibliográficas
\usepackage{url} % Manejo mejorado de URLs
\usepackage[hidelinks]{hyperref} % Hipervínculos en el documento, hidelinks quita los recuadros  de los links
\usepackage{multirow} % Permite combinar filas en tablas
\usepackage{array} % Mejoras en el entorno de tablas
\usepackage{fontawesome5} % Iconos de FontAwesome
\usepackage{tabularx} % Tablas con ancho automático
\usepackage{todonotes} % Permite insertar notas de tareas pendientes en el documento
\usepackage{placeins} % Permite usar el comando \FloatBarrier para controlar la posición de figuras y tablas flotantes
\usepackage{enumitem} % Permite personalizar listas (ítems, enumeraciones)
\usepackage{algorithm} % Entorno para escribir algoritmos
\usepackage{algpseudocode} % Entorno para escribir pseudocódigo dentro de algoritmos
\usepackage{tikz} % Diagramas y gráficos vectoriales
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, calc, babel} % Librerías de TikZ para diagramas

% Configuración para que los inputs/outputs de un algoritmo salgan en español
\renewcommand{\algorithmicrequire}{\textbf{Entrada:}}
\renewcommand{\algorithmicensure}{\textbf{Salida:}}
\makeatletter
\renewcommand{\ALG@name}{Algoritmo}
\makeatother

% Numeración de secciones personalizada (1.A.1) y de tablas (1,2,3...)
\renewcommand{\thesection}{\arabic{section}} 
\renewcommand{\thesubsection}{\thesection.\Alph{subsection}} 
\renewcommand{\thesubsubsection}{\thesubsection.\arabic{subsubsection}} 
\renewcommand{\thetable}{\arabic{table}}

% Configuración de metadatos 
\hypersetup{
    colorlinks=true,
    linkcolor=black, 
    citecolor=darkgray,
    urlcolor=blue,
    pdftitle={Comunicaciones seguras en el internet de las cosas en redes 5G},
}

% Traducción de etiquetas automáticas de IEEEtran al español
\def\IEEEkeywordsname{Palabras clave}
\def\IEEEproofname{Demostración}

\setlength{\marginparwidth}{10cm} % Configura el ancho del margen para todonotes

\geometry{a4paper, margin=0.5in}


\title{Comunicaciones seguras en el internet de las cosas en redes 5G}

\author{
  \IEEEauthorblockN{Francisco Gago Vázquez}
  \IEEEauthorblockA{
    \textit{Universidad de Sevilla}\\
    Sevilla, España\\
    fragagvaz@alum.us.es}
  \and
  \IEEEauthorblockN{Sergio García Eslava}
  \IEEEauthorblockA{
    \textit{Universidad de Sevilla}\\
    Sevilla, España\\
    sergaresl@alum.us.es}
  \and
  \IEEEauthorblockN{Marco Antonio Herrera Luján}
  \IEEEauthorblockA{
    \textit{Universidad de Sevilla}\\
    Sevilla, España\\
    marherluj@alum.us.es}
}

\begin{document}

\maketitle

% --- RESUMEN ---
\begin{abstract}
El despliegue de ciudades inteligentes requiere infraestructuras IoT eficientes, donde la tecnología Device-to-Device (D2D) sobre WiFi Direct juega un papel crucial para descongestionar y optimizar las redes 5G. Sin embargo, los protocolos estándar de WiFi Direct presentan vulnerabilidades críticas. Este trabajo se centra en localizar las fases vulnerables de la conexión y presentar los protocolos propuestos para mitigar los posibles ataques, garantizando la seguridad en el establecimiento de la conexión.
\end{abstract}

% --- PALABRAS CLAVE ---
\begin{IEEEkeywords}
IoT, Smart Cities, WiFi Direct, D2D, 5G, Ciberseguridad, Protocolo MAKE, SAS, RSSI, Commit/Open, Diffie-Hellman, DoS, Ciberseguridad, Autenticación mutua, Man-in-the-Middle, MAC (Message Authentication Code), MAC (Media Access Control), Nonces, Timestamp, Lifetime.
\end{IEEEkeywords}

% --- ÍNDICE ---
\tableofcontents
\bigskip
\hrule
\bigskip

% --- 1. INTRODUCCIÓN ---
\section{Introducción}
En la actualidad, se observa una evolución de las ciudades hacia soluciones más tecnológicas e inteligentes. Estas soluciones se aplican en diferentes contextos como la detección de niveles de basura en los contenedores, la monitorización de plazas de aparcamiento disponibles o la gestión eficiente de infraestructuras urbanas. Este avance se logra gracias a la integración de sensores electrónicos, móviles y sistemas de información a dispositivos físicos con la finalidad de optimizar las actividades cotidianas. 

En este escenario, el internet de las cosas (IoT), transforma objetos convencionales en objetos inteligentes, convirtiéndose en el motor de las Smart Cities.\cite{alfa2018role} Como consecuencia, se prevé que para 2025 haya 75 mil millones de dispositivos IoT generando datos masivos. Para soportar este volumen de datos, se requiere una infraestructura con un gran ancho de banda y una muy baja latencia. Las redes 5G, ya que cumplen con estas características concretas y además permiten conexiones Device-to-Device (D2D), una tecnología crucial en este contexto.\cite{gaba2021secure}

Las conexiones D2D son imprescindibles porque permiten a los dispositivos comunicarse directamente entre ellos sin la necesidad de pasar a través de una antena central que gestione las comunicaciones. Esto permite descongestionar la red y mejorar la eficiencia de la red. En redes 5G, estas comunicaciones pueden apoyarse tanto en servicios móviles como en Wi-Fi Direct. Sin embargo, el uso de los servicios móviles presenta limitaciones debido a la falta de cobertura en áreas urbanas. Por el otro lado, el WiFi Direct no tiene este problema, por lo que supone una ventaja significativa. Además, ofrece una transmisión de datos más rápida y menor latencia, siendo la opción más adecuada para implementar en las Smart Cities.

No obstante, el uso del WiFi Direct en conexiones D2D, tienen ciertos problemas de seguridad.\cite{shen2016secure} Al existir comunicación directa entre dispositivos, los atacantes pueden interferir en las comunicaciones de los dispositivos, permitiendo la escucha y modificación de mensajes y la suplantación de identidad. Las entidades están expuestas a diferentes ataques (Man-in-the-middle (MITM), DoS, etc..) lo que supone un riesgo importante. \cite{wang2017survey} Evidentemente, las entidades D2D requieren protección frente a este tipo de amenazas, haciendo necesario un protocolo seguro que evite estos problemas.

\subsection{Contribución a la investigación}
El articulo propone un protocolo de autenticación mutua y de establecimiento de claves seguro para el intercambio de información en comunicaciones Device-to-Device (D2D) sobre WiFi Direct en entornos IoT de ciudades inteligentes. La contribución de los autores se define en los siguientes cuatro fundamentos:
\begin{itemize}
    \item \textbf{Eficiencia y robustez:} El protocolo debe ser ligero, eficiente y robusto. Esto se consigue a través del uso de métodos primitivos y ligeros de la criptografía como son el protocolo Commit/Open, la criptografía simétrica, el intercambio de claves Diffie-Hellman y códigos de autenticación de mensaje (MAC).
    
    \item \textbf{Mitigación temprana de DoS:} Los dispositivos deben poder realizar un filtrado inteligente sobre las diferentes peticiones de conexión para evitar posibles ataques DoS, evitando el malgasto de recursos en la fase de descubrimiento.

    \item \textbf{Validación de seguridad:} El protocolo debe demostrarse que funciona mediante el análisis de Lógica BAN (Burrows-Abadi-Needham), asegurando que realmente es seguro. Consiguiendo esto los autores exhiben la necesidad del uso de propiedades como la frescura del mensaje, la autenticación mutua, etc.
    
    \item \textbf{Integridad y verificación de claves:} Debe resistir ataques MITM, DoS y de repetición, y asegurar que ambas partes verifican que la clave acordada es la misma para evitar errores de cifrado/descifrado.
\end{itemize}

% --- 2. CONTEXTO ---
\section{Contexto}

\subsection{WiFi-Direct}
El WiFi Direct es un protocolo de conexión de dispositivos, el cual permite a dos entidades conectarse sin la necesidad de un router externo. Esto permite una mayor transmisión de datos y una menor latencia, mejorando la eficiencia. Su funcionamiento se basa en las siguientes 4 fases (Fig. \ref{fig:diagrama_fases}):\cite{shen2016secure}
\begin{enumerate}
    \item \textbf{Fase de descubrimiento:} Es la fase inicial donde los dispositivos se detectan entre ellos para realizar las conexiones peer-to-peer(P2P). La búsqueda entre dispositivos se hace sin la necesidad de un router por el cual hacer las conexiones. 
    \item \textbf{Negociación de grupo:} Los dispositivos determinan quien actuará como Group Owner(GO), el cual es el que controla las comunicaciones del grupo. Este proceso se elige al GO, mediante un  handshake, donde cada participante del grupo P2P envía un “valor de intención”. El dispositivo que posea el valor intención más alto es quien se convierte en GO del grupo P2P.
    \item \textbf{Proceso WPS:} Se ejecuta el mecanismo de seguridad mediante WiFi Protected Setup (WPS), donde se lleva a cabo el acuerdo de claves y la autenticación mutua. Dentro del proceso de seguridad tradicional de WPS, el GO actúa como registrador para enviar las credenciales de seguridad de la red a las entidades del WiFi Direct. Tras esta fase, la protección del tráfico suele apoyarse en WPA (Wi-Fi Protected Access).
    \item \textbf{Configuración de dirección:} Finalmente una vez autenticados, el GO asigna direcciones IP (típicamente vía DHCP) y se establece el canal D2D operativo.
\end{enumerate}

\begin{figure*}[htbp]
\centering
\begin{tikzpicture}[
    % --- CONFIGURACIÓN DE FLECHAS ---
    >={Latex[width=2.5mm, length=3.5mm]}, 
    thick,
    font=\footnotesize,
    % Estilos de Cajas
    device/.style={draw, rectangle, minimum height=1cm, minimum width=2cm, rounded corners, font=\bfseries, fill=gray!10},
    msgbox/.style={midway, fill=white, inner sep=2pt, text=black, align=center, font=\small\bfseries, draw=none, rounded corners, rotate=90},
    % Estilo de Línea Discontinua (Gruesa y visible)
    linea/.style={draw, gray, line width=1.2pt, dash pattern=on 6pt off 6pt}
]
    \pgfdeclarelayer{bg}
    \pgfsetlayers{bg,main}

    % --- DEFINIR ACTORES ---
    \node[device] (A) at (0, 5) {Dispositivo A};
    \node[device] (B) at (0, 0) {Dispositivo B};

    % --- LÍNEAS DE TIEMPO (Ajustadas a 16.5cm para no sobresalir) ---
    % Antes era 17, ahora 16.5 coincide con el final del bloque rojo.
    \draw[linea] (A.east) -- ++(15.5,0) coordinate (A_end);
    \draw[linea] (B.east) -- ++(15.5,0) coordinate (B_end);

    % --- MENSAJES ---
    
    % -- FASE 1: DISCOVERY (x = 1.8, 2.8) --
    \draw[->, orange] (1.8, 5) -- node[msgbox] {Probe Request} (1.8, 0);
    \draw[<-, orange] (2.8, 5) -- node[msgbox] {Probe Response} (2.8, 0);

    % -- FASE 2: GO NEGOTIATION (x = 4.5, 5.5, 6.5) --
    \draw[->, blue] (4.5, 5) -- node[msgbox] {GO Neg. Request} (4.5, 0);
    \draw[<-, blue] (5.5, 5) -- node[msgbox] {GO Neg. Response} (5.5, 0);
    \draw[->, blue] (6.5, 5) -- node[msgbox] {GO Neg. Confirm} (6.5, 0);

    % -- FASE 3: WPS (x = 8.5, 9.8) --
    \draw[->, red] (8.5, 5) -- node[msgbox] {Authentication Req.} (8.5, 0);
    \draw[<->, red] (9.8, 5) -- node[msgbox] {EAPOL Key (4/4)} (9.8, 0);

    % -- FASE 4: ADDRESS CONFIG (x = 11.8, 13.0, 14.2, 15.4) --
    \draw[->, green] (11.8, 5) -- node[msgbox] {DHCP Discover} (11.8, 0);
    \draw[<-, green] (13.0, 5) -- node[msgbox] {DHCP Offer} (13.0, 0);
    \draw[->, green] (14.2, 5) -- node[msgbox] {DHCP Request} (14.2, 0);
    \draw[<-, green] (15.4, 5) -- node[msgbox] {DHCP ACK} (15.4, 0);

    % --- BARRAS DE FONDO ---
    \begin{pgfonlayer}{bg}
        % Discovery: 1.0 a 3.5
        \fill[orange!10] (1.0, 5.5) rectangle (3.5, -0.5);
        \node[text=orange!50!black, font=\bfseries] at (2.25, -0.8) {Discovery};

        % GO Negotiation: 3.5 a 7.5
        \fill[blue!10] (3.5, 5.5) rectangle (7.5, -0.5);
        \node[text=blue!50!black, font=\bfseries] at (5.5, -0.8) {GO Neg.};

        % WPS: 7.5 a 10.8
        \fill[red!10] (7.5, 5.5) rectangle (10.8, -0.5);
        \node[text=red!50!black, font=\bfseries] at (9.15, -0.8) {WPS};

        % Address Config: 10.8 a 16.5 (Termina justo aqui)
        \fill[green!10] (10.8, 5.5) rectangle (16.5, -0.5);
        \node[text=green!50!black, font=\bfseries] at (13.65, -0.8) {Address Config};
        
        % Separadores verticales
        \draw[gray!30] (3.5, 3.5) -- (3.5, -0.5);
        \draw[gray!30] (7.5, 3.5) -- (7.5, -0.5);
        \draw[gray!30] (10.8, 3.5) -- (10.8, -0.5);
    \end{pgfonlayer}

\end{tikzpicture}
\caption{Diagrama de secuencia detallado de la conexión WiFi Direct estándar.}
\label{fig:diagrama_fases}
\end{figure*}



\subsection{Protocolo de clave SAS}
El protocolo Short Authentication String (SAS) es un acuerdo de clave diseñado para la autenticación mutua y el establecimiento de claves en las comunicaciones Device-to-Device (D2D). Se apoya en  Diffie-Hellman como mecanismo fundamental para establecer una clave secreta. 

Además, utiliza un sistema de intercambio de claves basado en un esquema de compromiso (commitment scheme). El proceso de funcionamiento, basando la comunicación entre Alicia y Bob (Dispositivos A y B), incluye los siguientes pasos clave:

\begin{enumerate}
    \item \textbf{Generación de parámetros secretos:} Los dispositivos A y B generan sus identificadores ($ID_x$), parámetros Diffie Hellman ($g^x$) y números aleatorios: \textit{nonces} ($N_x$). Con ello construyen sus valores secretos ($m_x$) concatenando su identificador, el parámetro Diffie-Hellman y el nonce:
    \begin{equation}
        m_x = ID_x \parallel g^x \parallel N_x
    \end{equation}
    \item \textbf{Creación del Commit/Open Pair:} El dispositivo transforma el secreto $m_x$ en un par $(c, d)$, donde $c$ es el valor de compromiso y $d$ el valor de apertura.
    \item \textbf{Intercambio de Mensajes:}
    \begin{itemize}
        \item Inicialmente, Alice comparte su compromiso ($c$) con Bob.
        \item Como respuesta, Bob intercambia su valor secreto ($m_B$) con Alice.
        \item Alice, tras recibir el secreto de Bob ($m_B$), envía su valor de apertura ($d$) permitiendo a Bob extraer el valor secreto de Alice ($m_A$).
    \end{itemize}
    \item \textbf{Cálculo y verificación (SAS):} Tras obtener ambos dispositivos los secretos ($m_A, m_B$), ambos verifican la identidad mutua calculando la Short Authentication String (SAS), aplicando XOR a $N_A$ y $N_B$. Estas dos cadenas son enviadas a través de un canal confiable para ser comparadas. Si las cadenas de autenticación no coinciden ($S_A \neq S_B$), el proceso de establecimiento de claves se termina, asumiendo que existe un ataque Man-in-the-Middle (MITM).
    \item \textbf{Establecimiento de la clave:} Si la verificación es exitosa, ambos dispositivos calculan la clave secreta:
    \begin{equation}
        K = g^{ab} \pmod p
    \end{equation}
\end{enumerate}

A pesar de que el protocolo intenta prevenir el ataque MITM, este presenta una debilidad al compartir las cadenas de autenticación corta (SA, SB), ya que se comparte en texto simple a través de un canal inálambrico vulnerable, permitiendo así el ataque MITM mediante la escucha  (\textit{eavesdropping}).

\subsection{Limitaciones WPS}
El protocolo WiFi Protected Setup (WPS) es un mecanismo de seguridad utilizado por WiFi Direct para proteger las comunicaciones D2D (Device-to-Device). Este permite que el Group Owner (GO) registre las comunicaciones, para enviar las credenciales de seguridad a los clientes. Además, ofrece protección mediante dos métodos principales: el PIN y el botón de configuración (Push Button). Ambos presentan limitaciones que comprometen la robustez del protocolo:
\begin{enumerate}
    \item \textbf{Vulnerabilidad del PIN:} Se centra en un fallo del PIN de configuración, ya que este puede ser descubierto fácilmente mediante un ataque de fuerza bruta en una pequeña fracción de tiempo.\cite{viehbock2011wps} 
    \item \textbf{Vulnerabilidad del botón de configuración (Push Button):} En un escenario donde un atacante active su botón al mismo tiempo que el usuario, podría obtener acceso no autorizado al GO o al cliente.\cite{viehbock2011wps}
\end{enumerate}

Estas dos vulnerabilidades comprometen la seguridad de las comunicaciones D2D, provocando accesos y revelación de secretos en las comunicaciones.

% --- 3. OBJETIVOS DE SEGURIDAD ---
\section{Objetivos de seguridad}

El diseño de un protocolo criptográfico para infraestructuras críticas en Smart Cities debe resolver la tensión entre la necesidad de una seguridad robusta y las severas limitaciones de hardware de los dispositivos IoT. Para garantizar la viabilidad y confiabilidad del sistema propuesto, hemos formalizado los objetivos de seguridad basándonos en tres pilares fundamentales:\cite{gaba2021secure, wang2017survey}

\begin{itemize}
    \item \textbf{Disponibilidad:} El sistema debe garantizar la continuidad del servicio incluso en presencia de ataques activos de saturación. En el contexto de un semáforo inteligente, la incapacidad de procesar datos legítimos debido a un ataque DoS podría derivar en accidentes de tráfico o congestión urbana.
    
    \item \textbf{Integridad y autenticidad:} Se debe asegurar matemáticamente que la información recibida proviene de una fuente legítima y no ha sido manipulada en tránsito. La veracidad del dato es crítica; un atacante no debe poder inyectar estados de tráfico falsos.
    
    \item \textbf{Eficiencia computacional:} Debido a que los nodos IoT operan a menudo con baterías y CPUs de bajo consumo, los mecanismos de seguridad no deben introducir latencias que degraden el servicio ni agotar las reservas de energía prematuramente.
\end{itemize}

\subsection{Requisitos funcionales de seguridad}
Para satisfacer los objetivos anteriores, el protocolo debe cumplir con los siguientes requisitos criptográficos específicos:

\begin{itemize}
    \item \textbf{Autenticación Mutua en D2D:} 
    A diferencia de las redes centralizadas con una autoridad confiable, en WiFi Direct el canal es abierto. Es imperativo que tanto el emisor como el receptor verifiquen la identidad el uno del otro antes de intercambiar información sensible, evitando conexiones con nodos suplantadores.

    \item \textbf{Confidencialidad y acuerdo de claves:} 
    El protocolo debe establecer una clave de sesión única y efímera ($K_{ab}$) derivada de parámetros Diffie-Hellman. Esto garantiza que, aunque un atacante capture el tráfico cifrado, no podrá descifrar el contenido sin resolver el problema del logaritmo discreto.
    
    \item \textbf{Integridad de datos:} 
    Se requiere un mecanismo de vinculación criptográfica fuerte. Cualquier alteración en el payload (carga útil) del mensaje debe invalidar la verificación de la firma o el código MAC (Message Authentication Code), provocando el descarte inmediato del paquete.
    
    \item \textbf{Frescura del mensaje:} 
    El sistema debe ser inmune a ataques de repetición (\textit{Replay Attacks}). Un atacante no debe poder grabar un mensaje legítimo antiguo y reinyectarlo en la red horas después para causar disrupción. Esto requiere el uso de nonces aleatorios y marcas de tiempo (timestamps).
    
    \item \textbf{Resistencia a DoS en fase de descubrimiento:} 
    El protocolo debe incorporar mecanismos de filtrado temprano para descartar solicitudes maliciosas antes de que consuman recursos de procesamiento criptográfico costoso.
\end{itemize}

\text Aplicación al escenario de estudio:
Traducido a nuestro entorno de simulación, estos requisitos implican que el semáforo solo aceptará conexiones de cámaras que puedan probar posesión de un secreto compartido, que el estado del tráfico viaja cifrado para evitar espionaje, y que cualquier intento de inyectar una señal grabada previamente será rechazado por caducidad temporal.

\subsection{Modelo de adversario (Supuesto atacante)}
Para el análisis formal de seguridad, asumimos un adversario activo bajo el modelo estándar de Dolev-Yao. Este modelo, ampliamente aceptado en la verificación de protocolos, establece que "la red es el atacante". Las capacidades atribuidas al adversario son:

\begin{itemize}
    \item \textbf{Control total del canal:} El atacante puede escuchar (\textit{eavesdrop}), interceptar, modificar, inyectar y reordenar cualquier mensaje transmitido por el canal inalámbrico.
    
    \item \textbf{Asimetría de recursos:} A diferencia de los dispositivos IoT legítimos, asumimos que el atacante dispone de recursos computacionales (CPU/RAM) y energéticos significativamente superiores. Esto le permite ejecutar ataques de fuerza bruta limitados o mantener ataques de inundación (\textit{flooding}) sostenidos en el tiempo.
    
    \item \textbf{Conocimiento del protocolo:} Siguiendo el principio de Kerckhoffs, asumimos que el atacante conoce perfectamente los algoritmos y el funcionamiento del protocolo público, desconociendo únicamente las claves privadas y los números aleatorios (nonces) generados dinámicamente.
    
    \item \textbf{Objetivos Maliciosos:} El adversario busca tanto la interrupción del servicio (Ataques DoS para agotar batería o CPU) como la subversión de la comunicación (Ataques MITM para suplantar al semáforo o la cámara).
\end{itemize}

% --- 4. PROTOCOLO PROPUESTO ---
\section{Protocolo propuesto}
Las fases más propensas a ataques son Descubrimiento y Autenticación, como se muestra en la Fig. \ref{fig:diagrama_fases_redux}. El protocolo propone soluciones específicas para ellas.

\begin{figure*}[htbp]
\centering
\begin{tikzpicture}[
    % --- CONFIGURACIÓN DE FLECHAS ---
    >={Latex[width=2.5mm, length=3.5mm]}, 
    thick,
    font=\footnotesize,
    % Estilos de Cajas
    device/.style={draw, rectangle, minimum height=1cm, minimum width=2cm, rounded corners, font=\bfseries, fill=gray!10},
    msgbox/.style={midway, fill=white, inner sep=2pt, text=black, align=center, font=\small\bfseries, draw=none, rounded corners, rotate=90},
    % Estilo de Línea Discontinua (Gruesa y visible)
    linea/.style={draw, gray, line width=1.2pt, dash pattern=on 6pt off 6pt}
]
    \pgfdeclarelayer{bg}
    \pgfsetlayers{bg,main}

    % --- DEFINIR ACTORES ---
    \node[device] (A) at (0, 3) {Dispositivo A};
    \node[device] (B) at (0, 0) {Dispositivo B};

    % --- LÍNEAS DE TIEMPO (Ajustadas a 16.5cm para no sobresalir) ---
    % Antes era 17, ahora 16.5 coincide con el final del bloque rojo.
    \draw[linea] (A.east) -- ++(15.5,0) coordinate (A_end);
    \draw[linea] (B.east) -- ++(15.5,0) coordinate (B_end);

    % --- MENSAJES ---
    
    % -- FASE 1: DISCOVERY (x = 1.8, 2.8) --
    \draw[->, orange] (1.8, 3) -- (1.8, 0);
    \draw[<-, orange] (2.8, 3) -- (2.8, 0);

    % -- FASE 2: GO NEGOTIATION (x = 4.5, 5.5, 6.5) --
    \draw[->, black] (4.5, 3) -- (4.5, 0);
    \draw[<-, black] (5.5, 3) -- (5.5, 0);
    \draw[->, black] (6.5, 3) -- (6.5, 0);

    % -- FASE 3: WPS (x = 8.5, 9.8) --
    \draw[->, red] (8.5, 3) -- (8.5, 0);
    \draw[<->, red] (9.8, 3) -- (9.8, 0);

    % -- FASE 4: ADDRESS CONFIG (x = 11.8, 13.0, 14.2, 15.4) --
    \draw[->, gray] (11.8, 3) -- (11.8, 0);
    \draw[<-, gray] (13.0, 3) -- (13.0, 0);
    \draw[->, gray] (14.2, 3) -- (14.2, 0);
    \draw[<-, gray] (15.4, 3) -- (15.4, 0);

    % --- BARRAS DE FONDO ---
    \begin{pgfonlayer}{bg}
        % Discovery: 1.0 a 3.5
        \fill[orange!10] (1.0, 3.5) rectangle (3.5, -0.5);
        \node[text=orange!50!black, font=\bfseries] at (2.25, -0.8) {Discovery};

        % GO Negotiation: 3.5 a 7.5
        \fill[white!10] (3.5, 3.5) rectangle (7.5, -0.5);
        \node[text=black!50!black, font=\bfseries] at (5.5, -0.8) {GO Neg.};

        % WPS: 7.5 a 10.8
        \fill[red!10] (7.5, 3.5) rectangle (10.8, -0.5);
        \node[text=red!50!black, font=\bfseries] at (9.15, -0.8) {WPS};

        % Address Config: 10.8 a 16.5 (Termina justo aqui)
        \fill[gray!10] (10.8, 3.5) rectangle (16.5, -0.5);
        \node[text=gray!50!black, font=\bfseries] at (13.65, -0.8) {Address Config};
        
        % Separadores verticales
        \draw[gray!30] (3.5, 3.5) -- (3.5, -0.5);
        \draw[gray!30] (7.5, 3.5) -- (7.5, -0.5);
        \draw[gray!30] (10.8, 3.5) -- (10.8, -0.5);

        % --- ZONA DISCOVERY ---
    % Triángulo de peligro
    \node[text=orange!50!black, scale=2.5] at (2.3, 2.2) {\faExclamationTriangle};
    % Hacker (User Secret)
    \node[text=orange!50!black, scale=2.5] at (2.3, 0.4) {\faUserSecret};

    % --- ZONA WPS ---
    % Triángulo
    \node[text=red!50!black, scale=2.5] at (9.15, 2.2) {\faExclamationTriangle};
    % Hacker
    \node[text=red!50!black, scale=2.5] at (9.15, 0.4) {\faUserSecret};
    \end{pgfonlayer}

\end{tikzpicture}
\caption{Etapas críticas en la conexión WiFi Direct estándar.}
\label{fig:diagrama_fases_redux}
\end{figure*}

\subsection{Fase de descubrimiento}
Los dispositivos Wifi Direct deben escuchar \textit{probe requests}. Esto permite ataques DoS mediante solicitudes falsas masivas. Se plantea un mecanismo de filtrado.

\subsubsection{Suposiciones}
\begin{itemize}
    \item La dirección MAC es única.
    \item Se utiliza el RSSI (\textit{Received Signal Strength Indicator}) para estimar localización.
    \item Dispositivos distribuidos y estacionarios.
    \item Factores que afectan al RSSI se consideran constantes.
\end{itemize}

\subsubsection{Algoritmo}
El algoritmo asegura que no se procesen solicitudes de dispositivos con la misma MAC y mismo RSSI, o distinta MAC y mismo RSSI (spoofing).

\begin{algorithm}
\caption{Filtrado de solicitudes maliciosas en la fase de descubrimiento}
\begin{algorithmic}[1]
    \Require Dirección MAC, RSSI
    \Ensure Recursos, DoS
    \Statex \textbf{Abreviaturas:} new probe request (NPR), compares with database (CWD)    
    \State $D_1$ envía\_solicitud $D_2$ \Comment{$D_1$ envía solicitud de sondeo a $D_2$}
    \State $D_2$ recibe\_solicitud $D_1$ \Comment{$D_2$ almacena $MAC^1$, $RSSI^1$}
    
    \If{$MAC^2 == MAC^1$} \Comment{$MAC^2, RSSI^2 \in$ NPR}
        \If{$RSSI^2 == RSSI^1$} \Comment{$D_2$ realiza CWD}
            \State DoS() \Comment{Denegación de servicio}
        \EndIf
    \Else
        \If{$MAC^2 != MAC^1$}
            \If{$RSSI^2 != RSSI^1$}
                \State proveer\_servicio() \Comment{Recursos concedidos}
            \EndIf
        \Else
            \State DoS \Comment{Denegación de servicio}
        \EndIf
    \EndIf
\end{algorithmic}
\end{algorithm}

\subsection{Fase de Autenticación Mutua y Acuerdo de Clave}
El protocolo emplea el esquema \textbf{MAKE} (Mutual Authentication and Key Agreement) para prevenir MITM, mejorando SAS con:
\begin{itemize}
    \item Valores Timestamp (TS) y Lifetime (LT).
    \item SAS no se envía en texto plano.
    \item Verificación de generación de claves.
\end{itemize}

\textbf{Proceso de conexión (A con B):}
\begin{enumerate}
    \item Conocimiento inicial: IDs propios y ajenos, parámetros Diffie-Hellman.
    \item Generación de valores: aleatorios ($x$), $g^x$, nonce ($N$) y $TS/LT$.
    \item A genera par commit-open ($c, d$) y envía $c$ a B.
    \item Intercambio de claves públicas y apertura del compromiso.
    \item Cálculo de SAS y Claves Secretas ($K = g^{ab}$).
    \item Confirmación mediante MAC (Message Authentication Code) incluyendo TS y LT.
\end{enumerate}

\begin{algorithm}
\caption{MAKE - Mutual Authentication and Secret Key Establishment}\begin{algorithmic}[1]
    \Require IDs de dispositivos capturadas en fase de descubrimiento: $ID_A, ID_B$; Claves públicas: g, p; Cada dispositivo elige un valor secreto (s) y calcula $g^s \pmod p \{g^a \pmod p, g^b \pmod p\}$; Cálculo de Nonce $N_A$ y $N_B$ en disp. A y B respectivamente; Message Authentication Code (MAC); Timestamp (TS); Lifetime (LT)
    \Ensure MAKE Exitoso, MAKE Fallido
    
    \State $D_B \leftarrow c$ \Comment{Disp. A envía valor de compromiso al Disp. B}
    \State $D_A \leftarrow m_B$ \Comment{Disp. B envía $m_B$ al Disp. A}
    \State $D_B \leftarrow d$ \Comment{Disp. A envía valor de apertura al Disp. B}
    \State $S_A = N_A \oplus N'_B$ \Comment{Disp. A calcula cadena de autenticación}
    \State $S_B = N'_A \oplus N_B$ \Comment{Disp. B calcula cadena de autenticación}
    \State $K_{AB} = (g^b)^a \pmod p$ \Comment{Disp. A calcula clave secreta}
    \State $K_{BA} = (g^a)^b \pmod p$ \Comment{Disp. B calcula clave secreta}
    \State $D_B \leftarrow Auth_A = TS || LT || MAC_A(K_{AB}, S_A || TS || LT)$
    \State $D_A \leftarrow Auth_B = MAC_B(K_{BA}, S_B || TS || LT)$
    \If{$MAC_A == MAC_B$} \Comment{$D_A$ y $D_B$ verifican}
        \State MAKE Exitoso
    \Else
        \State MAKE Fallido
    \EndIf
\end{algorithmic}
\end{algorithm}

% --- 5. IMPLEMENTACIÓN Y DEMOSTRACIÓN ---
\section{Implementación y Demostración}

Para validar la viabilidad práctica de los protocolos teóricos presentados, se ha desarrollado un simulador interactivo basado en Python (FastAPI) y una interfaz web reactiva.\cite{codigo_fuente} Este entorno permite reproducir ataques reales y visualizar la eficacia de las contramedidas en tiempo real. Se adjunta al proyecto un \textit{Manual de Usuario} detallado para la ejecución del software.\cite{manual_usuario}

A continuación, se documentan los resultados obtenidos en los cuatro escenarios de prueba disponibles en la herramienta.

\subsection{Escenario 1: Ataque DoS (Vulnerable)}
En este modo, se simula un dispositivo IoT receptor (Cámara) sin mecanismos de filtrado previo. El objetivo es demostrar la fragilidad de los dispositivos con recursos limitados ante solicitudes masivas.

El proceso se inicia cuando el atacante (Hugo) lanza un ataque de inundación (\textit{flooding}).\cite{hadiks2014dos} Como se observa en la Fig. \ref{fig:dos_flood}, el adversario envía una ráfaga continua de paquetes de solicitud (REQ) hacia el dispositivo víctima, saturando el canal de comunicación.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{img/escenario1_flooding.png} 
    \caption{Inicio del ataque: El adversario inyecta múltiples solicitudes de conexión simultáneas hacia la víctima.}
    \label{fig:dos_flood}
\end{figure}

Al carecer de un algoritmo de validación en la fase de descubrimiento, la víctima intenta procesar cada una de estas solicitudes como si fueran legítimas. La Fig. \ref{fig:dos_logs} muestra los registros internos del sistema durante el ataque: inicialmente se aceptan los paquetes, pero la carga computacional escala rápidamente hasta agotar la capacidad de procesamiento, momento en el que se disparan las alertas de ``FALLO DEL SISTEMA''.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{img/escenario1_logs.png}
    \caption{Logs del sistema: La aceptación de todos paquetes lleva a la CPU a 0, provocando el fallo crítico.}
    \label{fig:dos_logs}
\end{figure}

La consecuencia final se ilustra en la Fig. \ref{fig:dos_colapso}. Una vez que la CPU alcanza el colapso total (0\%), el dispositivo queda inoperativo (representado por el icono de la cámara dañada), denegando el servicio a cualquier usuario legítimo que intente conectarse posteriormente.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{img/escenario1_colapso.png}
    \caption{Colapso del nodo: El dispositivo víctima queda fuera de servicio tras el agotamiento de recursos.}
    \label{fig:dos_colapso}
\end{figure}

\subsection{Escenario 2: Protección DoS (Propuesta)}
Para validar la robustez del algoritmo de filtrado contextual, se ha diseñado una prueba secuencial de cinco fases que evalúa la capacidad del sistema para distinguir entre usuarios legítimos y atacantes basándose en la identidad (MAC) y el contexto físico (RSSI).

En primer lugar, el dispositivo legítimo (Semáforo) solicita acceso con sus credenciales reales (Fig. \ref{fig:escenario2_envio1}: MAC terminada en \texttt{:66}) y una potencia de señal de -45 dBm. La Fig. \ref{fig:escenario2_logs1} muestra cómo el sistema verifica la solicitud y concede el acceso, estableciendo una sesión activa.

\begin{figure}[H]
    \centering
    \begin{minipage}{0.32\textwidth}
        \centering
        \includegraphics[width=\linewidth]{img/escenario2_envio1.png}
        \caption{ Solicitud legítima.}
        \label{fig:escenario2_envio1}
    \end{minipage}\hfill
    \begin{minipage}{0.66\textwidth}
        \centering
        \includegraphics[width=\linewidth]{img/escenario2_logs1.png}
        \caption{Resultado: Dispositivo verificado.}
        \label{fig:escenario2_logs1}
    \end{minipage}
\end{figure}

El atacante (Hugo el Hacker) intenta suplantar al semáforo copiando exactamente sus parámetros (Fig. \ref{fig:escenario2_envio2}): misma dirección MAC y mismo RSSI (-45 dBm). Como se observa en la Fig. \ref{fig:escenario2_logs2}, el protocolo detecta que esa MAC ya tiene una sesión activa registrada y bloquea inmediatamente el intento, protegiendo la integridad de la sesión en curso.

\begin{figure}[H]
    \centering
    \begin{minipage}{0.32\textwidth}
        \centering
        \includegraphics[width=\linewidth]{img/escenario2_envio2.png}
        \caption{Intento con misma MAC y RSSI.}
        \label{fig:escenario2_envio2}
    \end{minipage}\hfill
    \begin{minipage}{0.66\textwidth}
        \centering
        \includegraphics[width=\linewidth]{img/escenario2_logs2.png}
        \caption{Resultado: Bloqueo por sesión activa.}
        \label{fig:escenario2_logs2}
    \end{minipage}
ç\end{figure}

El atacante modifica su estrategia: mantiene la MAC suplantada pero altera la potencia de emisión a -67 dBm (Fig. \ref{fig:escenario2_envio3}), intentando simular un cambio de posición o fluctuación. La Fig. \ref{fig:escenario2_logs3} evidencia que el sistema no se deja engañar; al persistir el uso de una MAC ya registrada en el sistema, la solicitud sigue siendo rechazada ("BLOQUEADO") independientemente del cambio en la señal.

\begin{figure}[H]
    \centering
    \begin{minipage}{0.32\textwidth}
        \centering
        \includegraphics[width=\linewidth]{img/escenario2_envio3.png}
        \caption{Misma MAC con RSSI modificado.}
        \label{fig:escenario2_envio3}
    \end{minipage}\hfill
    \begin{minipage}{0.66\textwidth}
        \centering
        \includegraphics[width=\linewidth]{img/escenario2_logs3.png}
        \caption{Resultado: Persistencia del bloqueo.}
        \label{fig:escenario2_logs3}
    \end{minipage}
\end{figure}

En esta fase, el atacante cambia su identidad (Fig. \ref{fig:escenario2_envio4}), MAC nueva terminada en \texttt{:FF}, pero vuelve a emitir con la potencia original del semáforo (-45 dBm). El algoritmo de filtrado contextual identifica una anomalía física: es improbable que dos dispositivos distintos ocupen exactamente el mismo espacio radioeléctrico (mismo RSSI exacto). Por tanto, como muestra la Fig. \ref{fig:escenario2_logs4}, se bloquea la solicitud por "Distinta MAC en misma ubicación".

\begin{figure}[H]
    \centering
    \begin{minipage}{0.32\textwidth}
        \centering
        \includegraphics[width=\linewidth]{img/escenario2_envio4.png}
        \caption{Nueva MAC con RSSI ya almacenado.}
        \label{fig:escenario2_envio4}
    \end{minipage}\hfill
    \begin{minipage}{0.66\textwidth}
        \centering
        \includegraphics[width=\linewidth]{img/escenario2_logs4.png}
        \caption{Bloqueo por tener misma MAC en distinta ubicación.}
        \label{fig:escenario2_logs4}
    \end{minipage}
\end{figure}

Finalmente, se simula una conexión válida donde cambian tanto la identidad como el contexto. El dispositivo presenta una MAC nueva y un RSSI diferente (-19 dBm) (Fig. \ref{fig:escenario2_envio5}), lo cual es coherente con un nuevo nodo en la red. La Fig. \ref{fig:escenario2_logs5} confirma que el sistema valida estos parámetros al no encontrar conflictos, registrando exitosamente el nuevo dispositivo.

\begin{figure}[H]
    \centering
    \begin{minipage}{0.32\textwidth}
        \centering
        \includegraphics[width=\linewidth]{img/escenario2_envio5.png}
        \caption{Cambios en MAC y RSSI.}
        \label{fig:escenario2_envio5}
    \end{minipage}\hfill
    \begin{minipage}{0.66\textwidth}
        \centering
        \includegraphics[width=\linewidth]{img/escenario2_logs5.png}
        \caption{Resultado: Registro exitoso.}
        \label{fig:escenario2_logs5}
    \end{minipage}
\end{figure}


\subsection{Escenario 3: Ataque MitM en protocolo estándar (Vulnerable)}
En este escenario se evalúa la resistencia del protocolo estándar SAS frente a un atacante activo (Hugo). Para contextualizar el impacto del ataque, primero se presenta el funcionamiento nominal del sistema y posteriormente el ataque exitoso del Hacker.

\subsubsection*{Conexión legítima (Sin ataque)}
En condiciones normales, como se observa en la Fig. \ref{fig:sin_ataque}, Alice y Bob intercambian sus claves públicas Diffie-Hellman sin interferencias. El log confirma que ambos calculan la misma clave de sesión ($S_A = S_B = 11871$), completando la autenticación mutua de forma exitosa.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{img/SinAtaque.png} 
    \caption{Ejecución nominal: Alice y Bob derivan claves idénticas ($S_A=S_B$) sin interferencias.}
    \label{fig:sin_ataque}
\end{figure}

\subsubsection*{Ejecución del Ataque MitM}
La vulnerabilidad crítica se manifiesta cuando el atacante intercepta el canal. Como se evidencia en las Figs. \ref{fig:mitm_std_1} y \ref{fig:mitm_std_2}, Hugo se posiciona entre las víctimas:
\begin{enumerate}
    \item \textbf{Intercepción:} Hugo detiene los mensajes de compromiso ($M1, M2$) y reemplaza las claves públicas ($g_a, g_b$) por las suyas propias ($g_e$).
    \item \textbf{Suplantación:} Alice cree establecer una clave con Bob, pero en realidad establece $K_{AE} = 22$ con Hugo. Simultáneamente, Bob establece $K_{BE} = 20$ con Hugo.
    \item \textbf{Resultado:} El log final indica \texttt{MITM COMPLETADO}. Hugo posee ambas claves y puede descifrar y re-cifrar el tráfico entre Alice y Bob de manera transparente, comprometiendo totalmente la confidencialidad.
\end{enumerate}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{img/MITMEstandar1.png} 
    \caption{Fase inicial del ataque: Hugo intercepta los parámetros y suplanta las identidades de Alice y Bob.}
    \label{fig:mitm_std_1}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{img/MITMEstandar2.png} 
    \caption{Ataque exitoso: Se establecen dos claves de sesión distintas ($S_{AE} \neq S_{BE}$), otorgando al atacante control total del canal.}
    \label{fig:mitm_std_2}
\end{figure}


\subsection{Escenario 4: Protección MitM (Protocolo MAKE)}
Finalmente, se valida la eficacia de la propuesta MAKE. Este protocolo introduce verificación de integridad mediante MAC y sellado de tiempo para prevenir la manipulación de parámetros en tránsito.

Como se muestra en la secuencia de las Fig. \ref{fig:mitm_prot_1}, \ref{fig:mitm_prot_2} y \ref{fig:mitm_prot_3}, el atacante intenta replicar la estrategia anterior modificando el mensaje $M4$ para inyectar su clave ($g_e$). Sin embargo, el mecanismo de seguridad responde de la siguiente manera:

\begin{itemize}
    \item El protocolo detecta que la clave derivada por el atacante ($K_{AE}$) no coincide con la esperada por la víctima ($K_{BE}$).
    \item Al llegar al paso de verificación de autenticación (Mensaje $M11/M12$), Hugo es incapaz de falsificar un MAC válido porque desconoce el secreto compartido original.
    \item \textbf{Resultado:} El sistema lanza la alerta \texttt{BLOQUEADO [M12]}. La conexión se aborta inmediatamente con la razón \textit{"Hugo no puede crear MACs válidos"}, protegiendo a los usuarios de establecer un canal inseguro.
\end{itemize}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{img/MITMProtegido_1.png} 
    \caption{Fase de Inyección: El atacante intercepta el intercambio Diffie-Hellman e intenta manipular el mensaje M4 reemplazando la clave pública legítima ($g_b$) por la suya ($g_e$).}
    \label{fig:mitm_prot_1}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{img/MITMProtegido_2.png} 
    \caption{Desincronización de Claves: Como resultado de la manipulación, las víctimas derivan claves de sesión divergentes ($S_A \neq S_B$) que el atacante no puede unificar.}
    \label{fig:mitm_prot_2}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{img/MITMProtegido_3.png} 
    \caption{Prevención del Ataque: El mecanismo de seguridad verifica el MAC en el paso M12. Al detectar la firma inválida (debido a la clave incorrecta del atacante), el sistema bloquea la conexión instantáneamente.}
    \label{fig:mitm_prot_3}
\end{figure}

% --- 6. ANÁLISIS ---
\section{Análisis}
En esta sección se evalúa formalmente la robustez y eficiencia del protocolo propuesto, contrastando sus mecanismos de defensa con las vulnerabilidades inherentes al estándar WiFi Direct actual.

\subsection{Análisis de rendimiento - Mitigación de ataques DoS}
La principal diferencia en el rendimiento radica en la gestión de recursos durante la fase de descubrimiento.

\begin{itemize}
    \item \textbf{Protocolo estándar (Vulnerable):} El estándar actual carece de mecanismos de validación previa en la capa de descubrimiento. Esto obliga al dispositivo a asignar recursos de memoria y procesamiento a cualquier solicitud entrante (\textit{Probe Request}), independientemente de su legitimidad. Esta arquitectura es intrínsecamente susceptible a ataques de inundación, donde la saturación de la pila de protocolos provoca la indisponibilidad del servicio.
    \item \textbf{Protocolo propuesto (Eficiente):} La propuesta introduce una capa de abstracción que realiza un filtrado contextual basado en identificadores físicos y métricas de señal (RSSI). Teóricamente, esto permite descartar solicitudes maliciosas o repetitivas de forma inmediata y ligera, evitando que alcancen las capas superiores de procesamiento criptográfico y preservando así la estabilidad del sistema.
\end{itemize}

\subsection{Análisis de seguridad - Prevención MITM}
La seguridad del protocolo se fundamenta en la imposibilidad matemática de alterar los parámetros de negociación sin ser detectado.

\begin{itemize}
    \item \textbf{Protocolo estándar (Vulnerabilidad SAS):} Los protocolos basados en \textit{Short Authentication String} (SAS) tradicionales envían los compromisos o las cadenas de autenticación en claro o sin vinculación temporal fuerte. Un atacante con control del canal puede interceptar el intercambio y modificar los parámetros Diffie-Hellman al vuelo, estableciendo claves de sesión fraudulentas sin que las partes legítimas perciban la intrusión.
    
    \item \textbf{Protocolo propuesto (Integridad criptográfica):} El uso del esquema \textit{Commit/Open} obliga al iniciador a "comprometerse" con un valor oculto (hash) antes de revelar la clave pública. Esto elimina la ventana de oportunidad para ataques adaptativos. Además, la inclusión de marcas de tiempo (\textit{Timestamp}) y tiempo de vida (\textit{Lifetime}) en la firma MAC garantiza la frescura del mensaje, neutralizando teóricamente los ataques de repetición.
\end{itemize}

\subsection{Comparativa arquitectónica}
La Tabla \ref{tab:comparativa} resume las diferencias estructurales y sus implicaciones directas en la seguridad del sistema.

\begin{table*}[htbp]
    \centering
    \caption{Comparativa: Estándar vs. Propuesta}
    \label{tab:comparativa}
    \begin{tabular}{lcc}
        \hline
        \textbf{Propiedad} & \textbf{Estándar WiFi Direct} & \textbf{Protocolo propuesto} \\
        \hline
        Gestión de solicitudes & Procesamiento indiscriminado de cualquier petición. & Filtrado inteligente basado en el contexto. \\
        \hline
        Impacto DoS & Agotamiento de recursos (CPU y Batería). El sistema falla. & Rechazo inmediato. El sistema se mantiene estable. \\
        \hline
        Intercambio de claves & Vulnerable a ataques de intermediario (MITM). & Seguro gracias a la vinculación fuerte (Commit/Open). \\
        \hline
        Integridad & Verificación visual (el usuario compara códigos). & Verificación matemática automática. \\
        \hline
    \end{tabular}
\end{table*}

% --- 7. CONCLUSIONES ---
\section{Conclusiones}

El crecimiento exponencial de las Smart Cities plantea desafíos críticos de seguridad que no pueden ser resueltos mediante la infraestructura tradicional. Este trabajo ha demostrado que es posible asegurar las comunicaciones Device-to-Device (D2D) sobre WiFi Direct sin comprometer el rendimiento de dispositivos IoT con recursos limitados.

La validación teórica y experimental del protocolo propuesto permite extraer tres conclusiones fundamentales:

\begin{enumerate}
    \item \textbf{Resiliencia ante ataques de saturación:} La incorporación de un algoritmo de filtrado contextual en la fase de descubrimiento permite discriminar eficazmente el tráfico malicioso. A diferencia de los estándares actuales que procesan indiscriminadamente todas las solicitudes, nuestra propuesta mitiga los ataques de Denegación de Servicio (DoS) en una etapa temprana,\cite{hadiks2014dos} preservando la vida útil de la batería y la disponibilidad del servicio.
    
    \item \textbf{Integridad criptográfica ligera:} La implementación del esquema de compromiso (Commit/Open) junto con la autenticación mutua (MAKE) ha demostrado ser una solución robusta contra ataques de intermediario (Man-in-the-Middle).\cite{gaba2021secure} Al evitar el intercambio de claves en texto claro y garantizar la frescura de los mensajes mediante marcas de tiempo, se logra un canal seguro utilizando únicamente operaciones criptográficas ligeras (XOR, hash), ideales para el hardware restringido del IoT.
    
    \item \textbf{Viabilidad operativa:} Los resultados confirman que la seguridad no debe estar reñida con la eficiencia. El protocolo mantiene una latencia mínima y una alta estabilidad bajo estrés, validando su idoneidad para entornos críticos donde la inmediatez y la veracidad de los datos son imperativas.
\end{enumerate}

En definitiva, la solución presentada ofrece un equilibrio óptimo entre seguridad y rendimiento, estableciendo un marco de confianza necesario para el despliegue masivo de aplicaciones colaborativas en las ciudades del futuro.
% --- REFERENCIAS ---
\begin{thebibliography}{00}

\bibitem{gaba2021secure}
G.~S.~Gaba, G.~Kumar, T.-H.~Kim, H.~Monga, y P.~Kumar, ``Secure Device-to-Device communications for 5G enabled Internet of Things applications,'' \emph{Computer Communications}, vol. 169, pp. 114--128, Ene. 2021. DOI: \url{https://doi.org/10.1016/j.comcom.2021.01.010}

\bibitem{hadiks2014dos}
A.~Hadiks, Y.~Chen, F.~Li, y B.~Liu, ``A study of stealthy denial-of-service attacks in Wi-Fi Direct device-to-device networks,'' en \emph{2014 IEEE 11th Consumer Communications and Networking Conference (CCNC)}, IEEE, 2014, pp. 507--508. DOI: \url{https://doi.org/10.1109/CCNC.2014.6866617}

\bibitem{shen2016secure}
W.~Shen, B.~Yin, X.~Cao, L.~X.~Cai, y Y.~Cheng, ``Secure device-to-device communications over WiFi Direct,'' \emph{IEEE Network}, vol. 30, no. 5, pp. 4--9, 2016. DOI: \url{https://doi.org/10.1109/MNET.2016.7579020}

\bibitem{viehbock2011wps}
S.~Viehböck, ``Brute forcing Wi-Fi Protected Setup,'' \emph{Wi-Fi Protected Setup}, vol. 9, 2011. Disponible en: \url{https://sviehb.files.wordpress.com/2011/12/viehboeck_wps.pdf}

\bibitem{wang2017survey}
M.~Wang y Z.~Yan, ``A survey on security in D2D communications,'' \emph{Mobile Networks and Applications}, vol. 22, no. 2, pp. 195--208, 2017. DOI: \url{https://doi.org/10.1007/s11036-016-0741-5}

\bibitem{alfa2018role}
A.~S.~Alfa, B.~T.~Maharaj, H.~A.~Ghazaleh, y B.~Awoyemi, ``The role of 5G and IoT in Smart Cities,'' en \emph{Handbook of Smart Cities}, Springer, 2018, pp. 31--54. DOI: \url{https://doi.org/10.1007/978-3-319-97271-8_2}

\bibitem{manual_usuario}
F. Gago, S. Eslava, M. A. Herrera, ``Manual de Usuario: Protocolo MAKE para IoT,'' Documentación técnica, archivo \texttt{manual\_usuario.md} (Adjunto), 2025.

\bibitem{codigo_fuente}
F. Gago, S. Eslava, M. A. Herrera, ``Simulador-SmartCity'' Código fuente de la demostración práctica, archivo \texttt{Simulador-SmartCity.zip} (Adjunto), 2025.


\end{thebibliography}

\end{document}